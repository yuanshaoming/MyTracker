# 项目协作规则

## 任务范围

- 始终用中文沟通；代码标识符使用英文。
- 开始前读取本文件、README.md 和当前任务涉及的代码、文档。
- `docs/行人跟踪模块开发实施计划.md` 是设计参考，其中的示例、路线图和命令不代表当前已实现功能，也不授权一次执行全部开发阶段。
- 每次仅完成用户指定的任务。任务清单见 `docs/CODEX_TASKS_BATCH_01.md`。
- 编码前说明假设、修改范围和验证方法；影响公共接口或算法语义的歧义应先澄清。一般实现细节采用最简单的合理方案并说明。

## 架构边界

- 当前目录就是项目根目录，不再嵌套创建 `pedestrian_tracking/`。
- 使用 C++17、CMake；核心静态库目标统一命名为 `tracking_core`，命名空间为 `tracking`。
- 公共头文件放在 `include/tracking/`，内部实现放在 `src/`。
- 核心矩阵计算使用 Eigen3；GoogleTest 仅用于测试；OpenCV 仅用于可选可视化工具。
- 核心及公共接口不得依赖 Qt、OpenCV、CUDA、TensorRT、Python、PyTorch 或 ONNX Runtime。
- 检测器只通过标准 C++ Detection 数据输入；文件解析、图片读取、Qt 转换和业务逻辑位于核心之外。
- 一个 Tracker 实例只服务一个图片流，调用方串行调用 `update()` 和 `reset()`；不声称同一实例支持并发调用。
- 同一套核心代码支持 macOS、Windows 11 和 Jetson Xavier NX；平台适配不得侵入核心算法。

## 实现原则

- 最小实现，禁止为未来算法、平台或配置创建尚未需要的抽象。
- 先基础模块与 SORT，再 OC-SORT，最后评估 8 fps 优化；基础 SORT 不得标为完整 OC-SORT。
- 不引入 ReID、GPU 跟踪、跨摄像机身份、长时间遮挡身份恢复或额外业务功能。
- 参数集中在 `TrackerConfig`，只增加当前阶段实际消费的参数；计划中的数值是初始参考，不能称为设备验证结果。
- 区分固定步长预测与真实时间生命周期，不隐式假设输入永远为 125 ms。
- 只修改任务所需内容，保持已有风格；仅清理本次修改产生的无用代码。
- 不以静默吞错、返回伪造结果或放宽测试断言掩盖失败。
- 引入第三方代码前确认来源、版本和许可证，保留必要声明；依赖获取方式应明确，不在配置阶段偷偷联网下载。

## 验证与交付

- 修复缺陷先补充能复现问题的测试；算法模块测试覆盖正常输入和实际边界，使用明确的数值或行为断言。
- 浮点结果采用合理容差；不要依赖轨迹容器的偶然排列。
- 空检测帧仍须更新 Tracker。测试覆盖创建、匹配、丢失、删除、恢复和重置，以及时间戳异常契约。
- 完成当前任务的构建和相关测试；已有回归测试必须通过。无法执行时说明原因，不声称通过。
- 纯文档修改检查链接、命名、任务依赖和当前状态描述，不为文档新增程序测试。
- 完成后用中文概括改动、实际运行的验证及结果、未验证事项。
- 只把实际完成且有验收证据的任务标为完成；合成数据通过不等于真实现场或其他平台已验证。
- 不擅自初始化 Git、提交、推送或发布。
